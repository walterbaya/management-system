spring:
  application:
    name: sale
  batch:
    jdbc:
      initialize-schema: always

  datasource:
    url: jdbc:mysql://localhost:3306/palma_store
    username: root
    password: 1234
    driver-class-name: com.mysql.cj.jdbc.Driver

app:
 datetime:
 zone: America/Argentina/Buenos_Aires
 mysql-offset-hours: 3

